mutation draftOrderInvoiceSend($id: ID!) {
    draftOrderInvoiceSend(id: $id) {
        draftOrder {
            invoiceSentAt
            invoiceUrl
        }
        userErrors {
            message
        }
    }
}